# str = 'jayesh'
# rev = ''
# for i in str:
#     rev = i + rev
# print(rev)

# WAP to check reverse number

no = int(input("Enter a number: "))
rev = 0
while(no > 0):
    rev = (rev * 10) + no % 10
    no = no // 10
print(f"The reverse number is : {rev}")